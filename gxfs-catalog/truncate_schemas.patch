diff --git a/fc-service-core/src/main/java/eu/gaiax/difs/fc/core/service/schemastore/impl/SchemaStoreImpl.java b/fc-service-core/src/main/java/eu/gaiax/difs/fc/core/service/schemastore/impl/SchemaStoreImpl.java
index 3bf215d7..edac912e 100644
--- a/fc-service-core/src/main/java/eu/gaiax/difs/fc/core/service/schemastore/impl/SchemaStoreImpl.java
+++ b/fc-service-core/src/main/java/eu/gaiax/difs/fc/core/service/schemastore/impl/SchemaStoreImpl.java
@@ -69,11 +69,21 @@ public class SchemaStoreImpl implements SchemaStore {
 
   private static final Map<SchemaType, ContentAccessor> COMPOSITE_SCHEMAS = new ConcurrentHashMap<>();
 
+  private int truncateSchemas(Session session){
+    String hql = String.format("delete from SchemaRecord");
+    Query query = session.createQuery(hql);
+    return query.executeUpdate();
+  }
 
   @Override
   public int initializeDefaultSchemas() {
     Transaction tx = null;  
     try (Session session = sessionFactory.openSession()) {
+      tx = session.beginTransaction();
+      truncateSchemas(session);
+      session.flush();
+      tx.commit();
+
       int count = 0;
       Long found = session.createQuery("select count(s) from SchemaRecord s", Long.class).getSingleResult();
       if (found == 0) {
